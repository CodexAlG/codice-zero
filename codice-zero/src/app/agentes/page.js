import AgentsPageClient from './AgentsPageClient';

export const metadata = {
  title: 'Agentes Zenless Zone Zero | Tier List & Build - CodiceZero',
  description: 'Lista completa de todos los agentes de Zenless Zone Zero. Filtra por elemento, rango y facción. Encuentra las mejores builds en CodiceZero.',
};

export default function AgentsPage() {
  return <AgentsPageClient />;
}